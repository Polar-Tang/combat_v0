local require = require(script.Parent.loader).load(script)
local BaseObject = require("BaseObject")

local BaseAbility =  setmetatable({}, BaseObject)
BaseAbility.__index = BaseAbility

function BaseAbility.new()
	local self = setmetatable(BaseObject.new(), BaseAbility)

	return self
end

function BaseAbility:Execute(char)
	error("Execute must be overridden in subclass")
end


return BaseAbility
